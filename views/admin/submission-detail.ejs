<%- include('../partials/header', { title: title, currentPage: 'submissions', user: user }) %>

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Submission Details</h2>
        <div>
            <a href="/admin/submissions/<%= submission._id %>/edit" class="btn btn-primary me-2">
                <i class="fas fa-edit"></i> Edit
            </a>
            <button class="btn btn-danger me-2" onclick="deleteSubmission('<%= submission._id %>')">
                <i class="fas fa-trash"></i> Delete
            </button>
            <a href="/admin/submissions" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Back to Submissions
            </a>
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <h5 class="mb-0">Submission Information</h5>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-6 mb-3">
                    <strong>ID:</strong><br>
                    <code><%= submission._id %></code>
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Student:</strong><br>
                    <%= submission.userId ? (submission.userId.name || submission.userId.email || 'N/A') : 'N/A' %>
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Exam:</strong><br>
                    <%= submission.examId ? (submission.examId.name || 'N/A') : 'N/A' %>
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Status:</strong><br>
                    <span class="badge bg-<%= submission.status === 'graded' ? 'success' : submission.status === 'submitted' ? 'primary' : submission.status === 'late' ? 'warning' : 'secondary' %>">
                        <%= submission.status === 'graded' ? 'Graded' : submission.status === 'submitted' ? 'Submitted' : submission.status === 'late' ? 'Late' : 'In Progress' %>
                    </span>
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Score:</strong><br>
                    <%= submission.score || 0 %> / <%= submission.maxScore || 0 %>
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Percentage:</strong><br>
                    <%= submission.percentage || 0 %>%
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Time Spent:</strong><br>
                    <%= Math.floor((submission.timeSpent || 0) / 60) %> minutes
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Started At:</strong><br>
                    <%= submission.startedAt ? <% 
    const submission = new Date(submission.startedAt);
    const day = String(submission.getDate()).padStart(2, '0');
    const month = String(submission.getMonth() + 1).padStart(2, '0');
    const year = submission.getFullYear();
    const hours = String(submission.getHours()).padStart(2, '0');
    const minutes = String(submission.getMinutes()).padStart(2, '0');
    const seconds = String(submission.getSeconds()).padStart(2, '0');
  %><%= `${day}/${month}/${year} ${hours}:${minutes}:${seconds}` %> : 'N/A' %>
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Submitted At:</strong><br>
                    <%= submission.submittedAt ? <% 
    const submission = new Date(submission.submittedAt);
    const day = String(submission.getDate()).padStart(2, '0');
    const month = String(submission.getMonth() + 1).padStart(2, '0');
    const year = submission.getFullYear();
    const hours = String(submission.getHours()).padStart(2, '0');
    const minutes = String(submission.getMinutes()).padStart(2, '0');
    const seconds = String(submission.getSeconds()).padStart(2, '0');
  %><%= `${day}/${month}/${year} ${hours}:${minutes}:${seconds}` %> : 'N/A' %>
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Created At:</strong><br>
                    <%= <% 
    const submission = new Date(submission.createdAt);
    const day = String(submission.getDate()).padStart(2, '0');
    const month = String(submission.getMonth() + 1).padStart(2, '0');
    const year = submission.getFullYear();
    const hours = String(submission.getHours()).padStart(2, '0');
    const minutes = String(submission.getMinutes()).padStart(2, '0');
    const seconds = String(submission.getSeconds()).padStart(2, '0');
  %><%= `${day}/${month}/${year} ${hours}:${minutes}:${seconds}` %> %>
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Updated At:</strong><br>
                    <%= <% 
    const submission = new Date(submission.updatedAt);
    const day = String(submission.getDate()).padStart(2, '0');
    const month = String(submission.getMonth() + 1).padStart(2, '0');
    const year = submission.getFullYear();
    const hours = String(submission.getHours()).padStart(2, '0');
    const minutes = String(submission.getMinutes()).padStart(2, '0');
    const seconds = String(submission.getSeconds()).padStart(2, '0');
  %><%= `${day}/${month}/${year} ${hours}:${minutes}:${seconds}` %> %>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    function deleteSubmission(submissionId) {
        if (!confirm('Are you sure you want to delete this submission? This action cannot be undone.')) {
            return;
        }

        fetch(`/v1/api/admin/submissions/${submissionId}`, {
            method: 'DELETE',
            headers: {
                'Authorization': `Bearer ${getToken()}`
            }
        })
        .then(res => {
            if (!res.ok) {
                return res.json().then(err => Promise.reject(err));
            }
            return res.json();
        })
        .then(data => {
            if (data.ok) {
                showNotification('Submission deleted successfully!', 'success');
                window.location.href = '/admin/submissions';
            } else {
                showNotification('Error: ' + (data.message || 'Unknown error'), 'danger');
            }
        })
        .catch(err => {
            console.error('Error deleting submission:', err);
            showNotification('Error: ' + (err.message || 'Failed to delete submission'), 'danger');
        });
    }

    function getToken() {
        const cookies = document.cookie.split(';');
        for (let cookie of cookies) {
            const [name, value] = cookie.trim().split('=');
            if (name === 'admin_token') return value;
        }
        return '';
    }
</script>

<%- include('../partials/footer') %>

