<%- include('../partials/header', { title: title, currentPage: 'exams', user: user }) %>

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Exam Details</h2>
        <div>
            <a href="/admin/exams/<%= exam._id %>/edit" class="btn btn-primary me-2">
                <i class="fas fa-edit"></i> Edit
            </a>
            <button class="btn btn-danger me-2" onclick="deleteExam('<%= exam._id %>', '<%= exam.name %>')">
                <i class="fas fa-trash"></i> Delete
            </button>
            <a href="/admin/exams" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Back to Exams
            </a>
        </div>
    </div>

    <div class="card mb-4">
        <div class="card-header">
            <h5 class="mb-0">Exam Information</h5>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-6 mb-3">
                    <strong>ID:</strong><br>
                    <code><%= exam._id %></code>
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Name:</strong><br>
                    <%= exam.name || 'N/A' %>
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Owner:</strong><br>
                    <%= exam.ownerId ? (exam.ownerId.name || exam.ownerId.email || 'N/A') : 'N/A' %>
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Status:</strong><br>
                    <span class="badge bg-<%= exam.status === 'published' ? 'success' : 'warning' %>">
                        <%= exam.status === 'published' ? 'Published' : 'Draft' %>
                    </span>
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Duration:</strong><br>
                    <%= exam.duration || 0 %> minutes
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Total Marks:</strong><br>
                    <%= exam.totalMarks || 0 %>
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Questions:</strong><br>
                    <%= exam.questions ? exam.questions.length : 0 %>
                </div>
                <div class="col-md-12 mb-3">
                    <strong>Description:</strong><br>
                    <%= exam.description || 'N/A' %>
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Created At:</strong><br>
                    <% 
                        const examCreated = new Date(exam.createdAt);
                        const day = String(examCreated.getDate()).padStart(2, '0');
                        const month = String(examCreated.getMonth() + 1).padStart(2, '0');
                        const year = examCreated.getFullYear();
                        const hours = String(examCreated.getHours()).padStart(2, '0');
                        const minutes = String(examCreated.getMinutes()).padStart(2, '0');
                        const seconds = String(examCreated.getSeconds()).padStart(2, '0');
                    %><%= `${day}/${month}/${year} ${hours}:${minutes}:${seconds}` %>
                </div>
                <div class="col-md-6 mb-3">
                    <strong>Updated At:</strong><br>
                    <% 
                        const examUpdated = new Date(exam.updatedAt);
                        const day = String(examUpdated.getDate()).padStart(2, '0');
                        const month = String(examUpdated.getMonth() + 1).padStart(2, '0');
                        const year = examUpdated.getFullYear();
                        const hours = String(examUpdated.getHours()).padStart(2, '0');
                        const minutes = String(examUpdated.getMinutes()).padStart(2, '0');
                        const seconds = String(examUpdated.getSeconds()).padStart(2, '0');
                    %><%= `${day}/${month}/${year} ${hours}:${minutes}:${seconds}` %>
                </div>
            </div>
        </div>
    </div>

    <% if (exam.questions && exam.questions.length > 0) { %>
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">Questions (<%= exam.questions.length %>)</h5>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-sm">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Question ID</th>
                                <th>Points</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% exam.questions.forEach((q, index) => { %>
                                <tr>
                                    <td><%= index + 1 %></td>
                                    <td><code><%= q.questionId ? q.questionId._id : q.questionId %></code></td>
                                    <td><%= q.points || 0 %></td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    <% } %>
</div>

<script>
    function deleteExam(examId, examName) {
        if (!confirm(`Are you sure you want to delete exam "${examName}"? This action cannot be undone.`)) {
            return;
        }

        fetch(`/v1/api/exams/${examId}`, {
            method: 'DELETE',
            headers: {
                'Authorization': `Bearer ${getToken()}`
            }
        })
        .then(res => {
            if (!res.ok) {
                return res.json().then(err => Promise.reject(err));
            }
            return res.json();
        })
        .then(data => {
            if (data.ok) {
                showNotification('Exam deleted successfully!', 'success');
                window.location.href = '/admin/exams';
            } else {
                showNotification('Error: ' + (data.message || 'Unknown error'), 'danger');
            }
        })
        .catch(err => {
            console.error('Error deleting exam:', err);
            showNotification('Error: ' + (err.message || 'Failed to delete exam'), 'danger');
        });
    }

    function getToken() {
        const cookies = document.cookie.split(';');
        for (let cookie of cookies) {
            const [name, value] = cookie.trim().split('=');
            if (name === 'admin_token') return value;
        }
        return '';
    }
</script>

<%- include('../partials/footer') %>

